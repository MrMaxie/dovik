## ADDED Requirements

### Requirement: Consistent TUI key documentation

The operator documentation SHALL describe up and down for entity selection, left and right for workspace switching, `l` for refresh, and Enter as the primary action in every entity workspace.

#### Scenario: Learn the TUI interaction model

- **WHEN** a user reads the CLI and TUI guide
- **THEN** the guide presents one consistent key model for Processes, Identity, and Personas
