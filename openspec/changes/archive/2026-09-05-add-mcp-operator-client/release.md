---
impact: minor
visibility: public
components:
  - operator-clients
---

## Added

### Operate registered processes from MCP clients

Connect an MCP client to `dovik mcp` over stdio to list projects and processes, inspect status and captured output, and start, stop, or restart registered processes through the local daemon. The target version remains `1.0.0` in soak.
