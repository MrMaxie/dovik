---
impact: minor
visibility: public
components:
  - local-control-plane
  - operator-clients
---

## Added

### Control supervised processes through CLI or TUI

Dovik provides a complete non-interactive CLI with structured JSON output and a keyboard-driven terminal interface for navigating process state, running lifecycle actions, and following bounded stdout and stderr output through the same local daemon.
