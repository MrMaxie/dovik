name: npm distribution

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      release_tag:
        description: Existing GitHub Release tag to verify
        required: true
        type: string
      stage:
        description: Stage the verified packages for maintainer approval
        required: true
        default: false
        type: boolean

permissions:
  contents: read

concurrency:
  group: npm-${{ github.event.release.tag_name || inputs.release_tag }}
  cancel-in-progress: false

jobs:
  prepare:
    name: Prepare verified npm tarballs
    runs-on: ubuntu-22.04
    timeout-minutes: 20
    outputs:
      version: ${{ steps.release.outputs.version }}
    env:
      RELEASE_TAG: ${{ github.event.release.tag_name || inputs.release_tag }}
      GH_TOKEN: ${{ github.token }}
    steps:
      - name: Check out repository
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.0.3
        with:
          persist-credentials: false
      - name: Set up Node.js and npm
        uses: nubjs/setup-nub@21b317c170eee658503996209cf87b63481be35a # v0
        with:
          node-version: 24.18.0
          nub-version: 0.7.5
          package-manager-cache: false
      - name: Pin npm CLI
        run: npm install --global npm@12.0.2 --ignore-scripts
      - name: Verify toolchain
        run: |
          test "$(node --version)" = "v24.18.0"
          test "$(npm --version)" = "12.0.2"
      - name: Validate release tag
        id: release
        shell: bash
        run: |
          if [[ ! "$RELEASE_TAG" =~ ^v([0-9]+\.[0-9]+\.[0-9]+([.-][0-9A-Za-z.-]+)?)$ ]]; then
            echo "Invalid Dovik release tag: $RELEASE_TAG" >&2
            exit 1
          fi
          version="${BASH_REMATCH[1]}"
          test "$(node -p \"require('./package.json').version\")" = "$version"
          echo "version=$version" >> "$GITHUB_OUTPUT"
      - name: Check package sources and workflow
        run: npm run check:npm
      - name: Download exact GitHub Release assets
        run: gh release download "$RELEASE_TAG" --repo "$GITHUB_REPOSITORY" --dir "$RUNNER_TEMP/dovik-assets"
      - name: Assemble packages from attested release archives
        run: >-
          node scripts/assemble-npm-release.mjs
          --assets "$RUNNER_TEMP/dovik-assets"
          --output "$RUNNER_TEMP/dovik-npm-stage"
          --version "${{ steps.release.outputs.version }}"
      - name: Pack deterministic npm tarballs
        run: >-
          node scripts/pack-npm-release.mjs
          --stage "$RUNNER_TEMP/dovik-npm-stage"
          --output "$RUNNER_TEMP/dovik-npm-tarballs"
      - name: Upload verified npm tarballs
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: dovik-npm-${{ steps.release.outputs.version }}
          path: ${{ runner.temp }}/dovik-npm-tarballs
          if-no-files-found: error
          retention-days: 14

  verify-native:
    name: Verify npm consumer (${{ matrix.target }})
    needs: prepare
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: win32-x64
            runner: windows-2022
          - target: linux-x64
            runner: ubuntu-22.04
          - target: darwin-x64
            runner: macos-15-intel
          - target: darwin-arm64
            runner: macos-15
    runs-on: ${{ matrix.runner }}
    steps:
      - name: Check out repository
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.0.3
        with:
          persist-credentials: false
      - name: Set up Node.js and npm
        uses: nubjs/setup-nub@21b317c170eee658503996209cf87b63481be35a # v0
        with:
          node-version: 24.18.0
          nub-version: 0.7.5
          package-manager-cache: false
      - name: Pin npm CLI
        run: npm install --global npm@12.0.2 --ignore-scripts
      - name: Download npm tarballs
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v7.0.0
        with:
          name: dovik-npm-${{ needs.prepare.outputs.version }}
          path: ${{ runner.temp }}/dovik-npm-tarballs
      - name: Verify isolated native installation
        run: node scripts/verify-npm-consumer.mjs --tarballs "${{ runner.temp }}/dovik-npm-tarballs"

  stage:
    name: Stage npm packages for 2FA approval
    needs: [prepare, verify-native]
    if: github.event_name == 'release' || inputs.stage == true
    runs-on: ubuntu-22.04
    timeout-minutes: 15
    environment: npm-production
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Check out repository
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.0.3
        with:
          persist-credentials: false
      - name: Set up Node.js and npm
        uses: nubjs/setup-nub@21b317c170eee658503996209cf87b63481be35a # v0
        with:
          node-version: 24.18.0
          nub-version: 0.7.5
          package-manager-cache: false
      - name: Pin npm CLI
        run: npm install --global npm@12.0.2 --ignore-scripts
      - name: Download verified npm tarballs
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v7.0.0
        with:
          name: dovik-npm-${{ needs.prepare.outputs.version }}
          path: ${{ runner.temp }}/dovik-npm-tarballs
      - name: Stage platform packages and root package
        run: node scripts/stage-npm-release.mjs --tarballs "${{ runner.temp }}/dovik-npm-tarballs"
      - name: Show approval order
        run: |
          echo 'Review the five entries in npm Staged Packages.' >> "$GITHUB_STEP_SUMMARY"
          echo 'Approve the four @maxiedev platform packages before approving dovik.' >> "$GITHUB_STEP_SUMMARY"
          echo 'CLI alternative: npm stage approve <stage-id>' >> "$GITHUB_STEP_SUMMARY"
